{{#if isOpen}}
<div class="share-modal-body">

  <div class="share-modal-header">
    <h1>Share document</h1>
  </div>

  <div class="share-modal-content">
    {{#unless isLoaded}}
      <em>Retrieving data...</em>
    {{/unless}}
    {{#if showError}}
      <em>Error retrieving permissions for this document.</em>
    {{/if}}
    {{#if showData}}
      <h2>Users with access to document</h2>
      <ul class="permission-list">
        {{#each permission in permissions}}
        <li class="permission-item">
          <span>{{#if permission.name}}{{permission.name}} ({{permission.emailAddress}}){{else}}{{permission.emailAddress}}{{/if}}</span>
          <a {{action 'removePermission' permission}}></a>
        </li>
        {{/each}}
      </ul>
      <h2>Share with more users</h2>
      <form action 'share' on="submit">
        {{input placeholder="Type e-mail" value=shareAddress }}
        <input type="submit" value="Share" {{action 'share'}}/>
      </form>
    {{/if}}
  </div>

  <div class="share-modal-footer">
    <button {{action 'close'}} class="button regular">Close</button>
  </div>
</div>
{{/if}}
